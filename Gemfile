source 'https://rubygems.org'

# Specifying the ruby version is only possible in the newest version of the bundler. If you get a
# problem here, use gem install bundler --pre to install the newest version of the bundler gem.
ruby "1.9.3"

gem 'rails', '3.2.8'

gem 'thin'

# Gems used only for assets and not required
# in production environments by default.
group :assets do
  gem 'sass-rails',   '~> 3.2.3'
  gem 'coffee-rails', '~> 3.2.1'

  # See https://github.com/sstephenson/execjs#readme for more supported runtimes
  # gem 'therubyracer', :platforms => :ruby

  gem 'uglifier', '>= 1.0.3'
end

gem 'jquery-rails'

gem 'less-rails-bootstrap', '>= 2.1.1'

gem "therubyracer", :require => 'v8'


# To use ActiveModel has_secure_password
# gem 'bcrypt-ruby', '~> 3.0.0'

# To use Jbuilder templates for JSON
# gem 'jbuilder'

# Use unicorn as the app server
# gem 'unicorn'

# Deploy with Capistrano
# gem 'capistrano'

# To use debugger
# gem 'debugger'
